<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sa.tvtc.projectstracking" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/company_small_icon"
        android:label="@string/application_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="sa.tvtc.activities.MainActivity"
            android:label="@string/application_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="sa.tvtc.activities.LoginActivity"/>
        <activity android:name="sa.tvtc.activities.HomeActivity"/>
        <activity android:name="sa.tvtc.activities.ProjectDetailsActivity"/>
        <activity android:name="sa.tvtc.activities.AddFollowUpActivity"/>
    </application>

</manifest>
